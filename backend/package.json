{
  "name": "pepnationrx-backend",
  "version": "0.1.0",
  "description": "Scaffolded telehealth MSO backend for PepNationRX — integrates SteadyMD, Empower Pharmacy, and Hallandale Pharmacy with conditional pharmacy routing.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development SERVE_STATIC=true node --watch src/server.js",
    "dev:api-only": "NODE_ENV=development node --watch src/server.js",
    "test": "node scripts/test-intake.js && PNRX_NO_LISTEN=1 node scripts/test-auth.js && PNRX_NO_LISTEN=1 node scripts/test-stripe.js",
    "test:intake": "node scripts/test-intake.js",
    "test:auth": "PNRX_NO_LISTEN=1 node scripts/test-auth.js",
    "test:stripe": "PNRX_NO_LISTEN=1 node scripts/test-stripe.js"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^17.5.0",
    "zod": "^3.23.8"
  }
}
